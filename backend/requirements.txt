fastapi>=0.110
uvicorn[standard]>=0.29
# 锁 0.0.39（与本地测试环境一致）。0.0.40+ 要求 datetime 必须带时区信息，
# 现有代码用 naive datetime.utcnow()，升级后登录等写库会抛 500
#（"Datetime values must have timezone information"）。
sqlmodel==0.0.39
requests>=2.31
# 认证：JWT 签发/校验、bcrypt 密码哈希、.env 配置加载
python-jose[cryptography]>=3.3
bcrypt>=4.0
python-dotenv>=1.0
# 行情数据源。pandas 锁 2.x，避免 akshare 与 pandas 3 不兼容。
# Windows + Microsoft Store Python 安装若卡在 jsonpath 构建，
# 安装前设环境变量： SETUPTOOLS_USE_DISTUTILS=stdlib
akshare>=1.12
pandas>=2,<3
# 测试
pytest>=8.0
httpx>=0.27
openai>=2.0.0
cryptography>=41.0
python-multipart
# PostgreSQL 支持（可选；默认 SQLite，通过 DATABASE_URL 切换到 PG 时需要此驱动）
psycopg2-binary>=2.9
